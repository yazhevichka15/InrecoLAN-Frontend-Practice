import { BrowserRouter } from "react-router-dom";
import GridCatalog from "../pages/grid-catalog/GridCatalog";

function App() {
  return (
    <BrowserRouter>
      <GridCatalog />
    </BrowserRouter>
  );
}

export default App;